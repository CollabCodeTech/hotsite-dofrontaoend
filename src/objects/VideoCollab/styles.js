import styled from 'styled-components'

export const Video = styled.div`
    background-color: var(--color-second);
    width: 528px;
    height: 297px;
    border-radius: var(--radius-small);
    box-shadow: 2px 2px 4px var(--color-base);
`
